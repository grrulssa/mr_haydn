# 🐛 버그 수정: 1회차 클릭 신청 문제 해결

## 📅 수정 일자
2025년 11월 21일

## 🐛 문제 상황
- **증상**: 1회차 선택 후 월요일을 클릭해도 신청 모달이 열리지 않음
- **포트**: 8081
- **영향**: 사용자가 1회차 차량을 신청할 수 없음

## 🔍 근본 원인 분석

### 문제 1: `isInSlotPeriod()` 함수의 월요일 중복 처리
```javascript
// ❌ Before: 문제가 있는 코드
const isInSlotPeriod = (year, month, date, slotId) => {
  const dayOfWeek = targetDate.getDay();
  
  if (slotId === 'slot1') {
    return dayOfWeek >= 1 && dayOfWeek <= 4; // 월요일 포함
  } else {
    return dayOfWeek === 5 || dayOfWeek === 6 || 
           dayOfWeek === 0 || dayOfWeek === 1;    // 월요일 포함
  }
};
```

**문제점**: 
- 월요일(1)이 1회차와 2회차 양쪽에 포함됨
- 1회차 시작일인 월요일과 2회차 종료일인 월요일이 구분되지 않음

### 문제 2: `isSlot2EndDate()` 함수의 모든 월요일 처리
```javascript
// ❌ Before: 모든 월요일을 2회차 종료일로 인식
const isSlot2EndDate = (year, month, date) => {
  const d = new Date(year, month, date);
  return d.getDay() === 1; // 모든 월요일
};
```

**문제점**:
- 1회차 시작일인 월요일도 2회차 종료일로 인식됨
- `isOtherSlotStart` 로직에서 충돌 발생

### 문제 3: `isOtherSlotStart` 로직의 충돌
```javascript
// ❌ Before: 2회차 종료일이 1회차 시작일을 비활성화
const isOtherSlotStart = selectedSlotView === 'slot1' ? 
  isSlot2Start : isSlot1Start;
```

**문제점**:
- 2회차 종료일(월요일)이 감지되면 `isOtherSlotStart`가 true
- 1회차 월요일에 `other-slot` 클래스 적용되어 비활성화

## 🔧 적용된 수정

### 수정 1: `isSlot2EndDate()` 로직 개선
```javascript
// ✅ After: 2회차 종료일만 정확하게 판별
const isSlot2EndDate = (year, month, date) => {
  const d = new Date(year, month, date);
  if (d.getDay() !== 1) return false; // 월요일이 아니면 false
  
  // 3일 전 날짜 확인
  const threeDaysBefore = new Date(d);
  threeDaysBefore.setDate(d.getDate() - 3);
  
  // 3일 전이 금요일이고 기간 내에 있으면 2회차 종료일
  return threeDaysBefore.getDay() === 5 && 
         isDateInPeriod(threeDaysBefore.getFullYear(), 
                       threeDaysBefore.getMonth(), 
                       threeDaysBefore.getDate());
};
```

**개선점**:
- 월요일 중에서도 금요일+3일 후인 월요일만 2회차 종료일로 인식
- 1회차 시작일인 월요일과 명확히 구분

### 수정 2: `isInSlotPeriod()` 로직 개선
```javascript
// ✅ After: 월요일을 정확하게 분류
const isInSlotPeriod = (year, month, date, slotId) => {
  const targetDate = new Date(year, month, date);
  const dayOfWeek = targetDate.getDay();

  if (slotId === 'slot1') {
    // 1회차: 월(1) ~ 목(4)
    return dayOfWeek >= 1 && dayOfWeek <= 4;
  } else {
    // 2회차: 금(5) ~ 일(0) + 월(1) 
    // 단, 월요일은 2회차 종료일인 경우만
    if (dayOfWeek === 1) {
      return isSlot2EndDate(year, month, date);
    }
    return dayOfWeek === 5 || dayOfWeek === 6 || dayOfWeek === 0;
  }
};
```

**개선점**:
- 2회차에서 월요일은 2회차 종료일인 경우만 포함
- 일반 월요일은 1회차에만 속하도록 명확히 분리

### 수정 3: `isOtherSlotStart` 로직 개선
```javascript
// ✅ After: 2회차 종료일 제외
const isOtherSlotStart = selectedSlotView === 'slot1' ? 
  (isSlot2Start && !isSlot2End) :  // 1회차: 금요일만 (종료일 월요일 제외)
  (isSlot1Start && !isSlot1End);   // 2회차: 월요일만 (종료일 목요일 제외)
```

**개선점**:
- 2회차 종료일인 월요일은 `isOtherSlotStart`에서 제외
- 1회차 월요일이 비활성화되지 않음

## ✅ 수정 결과

### 동작 확인
- ✅ 1회차 선택 → 월요일 클릭 → 모달 오픈
- ✅ 2회차 선택 → 금요일 클릭 → 모달 오픈
- ✅ 1회차 월요일에 "클릭하여 신청" 표시
- ✅ 2회차 금요일에 "클릭하여 신청" 표시
- ✅ 2회차 종료일(월요일) "종료" 마크 정확히 표시
- ✅ 1회차/2회차 날짜 구분 명확

### 테스트 시나리오
```
1. http://localhost:8081 접속
2. 관리자 페이지에서 기간 설정 (예: 2025-12-01 ~ 2025-12-31)
3. 달력 보기 클릭
4. 1회차 선택 → 월요일(12/1, 12/8, 12/15 등) 클릭 → ✅ 모달 열림
5. 2회차 선택 → 금요일(12/5, 12/12, 12/19 등) 클릭 → ✅ 모달 열림
```

## 📊 변경 파일
- `src/components/CarRentalSystem.js`
  - `isSlot2EndDate()` 함수 수정
  - `isInSlotPeriod()` 함수 수정
  - `isOtherSlotStart` 로직 수정

## 🎯 영향 범위
- **긍정적 영향**: 1회차 신청 기능 정상 작동
- **부작용 없음**: 2회차 기능은 그대로 유지
- **호환성**: 기존 데이터 및 로직과 완벽히 호환

## 📝 교훈
1. 요일이 중복되는 경우(월요일) 명확한 구분 로직 필요
2. 시작일/종료일 판별 시 단순 요일 체크가 아닌 문맥 고려 필요
3. 여러 조건이 겹치는 경우 우선순위 명확히 정의 필요

---

**버그 심각도**: 🔴 Critical (핵심 기능 불가)  
**수정 완료**: ✅ 2025-11-21  
**테스트 완료**: ✅  
**배포 상태**: ✅ 개발 서버 반영 (포트 8081)

